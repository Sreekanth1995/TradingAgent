#!/usr/bin/expect -f
set timeout -1
set password $env(DEPLOY_PASS)
spawn bash deploy_vultr.sh
expect {
    -nocase "password:" {
        send "$password\r"
        exp_continue
    }
    "Are you sure you want to continue connecting" {
        send "yes\r"
        exp_continue
    }
    eof
}
